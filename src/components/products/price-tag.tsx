/**
 * PriceTag component — National Plasto catalogue is 100% inquiry-based.
 * Prices have been completely removed per requirements.
 */
export function PriceTag(_props?: Record<string, unknown>) {
  return null;
}
